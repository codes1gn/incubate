# Scenario 02 — valid-idea-cursor

**What it validates:** SKILL.md documents the Cursor IDE incubation path.

## Checks
- Cursor platform documented in SKILL.md
- git CLI path (`git init`, `git push`) is documented
- Cursor memory path (`~/.cursor/skills/incubate/data/`) is documented
- Windows path support (`%USERPROFILE%`) is documented
- Platform compatibility matrix is present
- Cursor install path is documented
